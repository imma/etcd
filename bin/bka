#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"
  source normalize  

  exec buildkite-agent start --bootstrap-script ${shome}/bootstrap.sh --build-path ${shome}/builds --hooks-path ${shome}/hooks "$@"
}

main "$@"
